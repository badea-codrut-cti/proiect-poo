export default {
    multipass: true, 
    plugins: [
        "preset-default",
        "mergeStyles",
        "mergePaths",
        "prefixIds",
        "cleanupIds",
        "removeEmptyAttrs"
    ]
}